@page
@model WebClinic.Pages.LoginModel

@{
    ViewData["Title"] = "Авторизация";
}

@* <div class="container align-middle">
    <div class="w-50 col-md-8 mx-auto" style="text-align: center">
        <form asp-controller="Authorization" asp-action="Index" method="post">
            <div class="form-group m-3">
                <input class="form-control item" type="text" id="login" placeholder="Логин" asp-for="Login">
                <div>
                    <span class="text-danger" asp-validation-for="Login"></span>
                </div>
            </div>
            <div class="form-group m-3">
                <input class="form-control item" type="password" id="password" placeholder="Пароль" asp-for="Pass">
                <div>
                    <span class="text-danger" asp-validation-for="Pass"></span>
                </div>
            </div>
            <div class="form-group m-3">
                <button class="btn btn-success btn-block create-account" type="submit">Вход в аккаунт</button>
                <a class="btn btn-outline-primary btn-block create-account" asp-controller="Authorization" asp-action="RegistrationPatient">Регистрация</a>
            </div>
        </form>
    </div>
    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            @TempData["SuccessMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }
</div> *@